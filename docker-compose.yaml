version: '3'

services:
  app:
    container_name: php
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/var/www
    networks:
      - backend
    ports:
      - 8000:9000
networks:
  backend:
    name: backend
    driver: bridge